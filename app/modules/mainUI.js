define(function(require, exports, module) {
    "use strict";
    // 获取相关依赖

    var mainUI = function(){
        var menuModule = require('menu');
        var homeModule = require('home');
    };
    return mainUI;
});
