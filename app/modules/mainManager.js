define(function(require, exports, module) {
    "use strict";
    // 获取相关依赖

    var util = require('util');

    var mainManager = {
        // 每个模块带个模块ID
        module: 'mainManager'
    };

    return mainManager;
});
