define(function(require, exports, module) {
    "use strict";
    // 获取相关依赖

    var util = require('util');

    var popManager = {
        // 每个模块带个模块ID
        module: 'popManager'
    };

    return popManager;
});
